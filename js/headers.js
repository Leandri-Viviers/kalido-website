$(document).ready(function(){
	//	Typed.js
	$('#client-goal').typed({
		strings: ['piano lessons','dog grooming','legal advice', 'garden services', 'pizza-appreciation tours'],
		contentType: 'text',
		typeSpeed: 50,
		startDelay: 200,
		backDelay: 2000,
		loop: true
	});

	$('#professional-goal').typed({
		strings: ['a web developer','a good dentist','a yoga instructor', 'a maths tutor', 'a fortune cookie writer'],
		contentType: 'text',
		typeSpeed: 50,
		startDelay: 200,
		backDelay: 2000,
		loop: true
	});

	$('#anyone-goal').typed({
		strings: ['a mentor from Harvard','a running buddy','a personal stylist', 'a sommelier', 'a professional mime'],
		contentType: 'text',
		typeSpeed: 50,
		startDelay: 200,
		backDelay: 2000,
		loop: true
	});

	$('#index-goal').typed({
		strings: ['a web developer','a good dentist','a yoga instructor', 'a maths tutor', 'a fortune cookie writer'],
		contentType: 'text',
		typeSpeed: 50,
		startDelay: 200,
		backDelay: 2000,
		loop: true
	});
});